---
const { 
    class: className = '', 
} = Astro.props;
---

<p class={`text-header text-3xl sm:text-4xl lg:text-5xl font-bold text-white dark:text-neutral-950 uppercase ${className}`}>
	<slot />
</p>